# Screenshots

Drop the following PNGs here for the main README to render them:

- `main.png` — full app view, ideally on a wide window
- `strategy-mode.png` — strategy mode with the chart populated
- `manual-mode.png` — manual mode with a few markers placed
- `params.png` — close-up of the parameter sliders

Recommended size: 2400×1500 (retina-ish) or larger. The README references these by relative path so they'll work on GitHub and Netlify identically.
